import { DigitSequenceEditorComponent } from './digit-sequence-editor.component';

describe('DigitSequenceEditorComponent', () => {
  it('should create an instance', () => {
    const directive = new DigitSequenceEditorComponent();
    expect(directive).toBeTruthy();
  });
});
